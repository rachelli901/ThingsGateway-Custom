# 候选任务介绍

## 基本信息
- 安全任务 ID：T03
- 脱敏标题：本地 Coding Agent 轨迹筛选与结构化打包（会话未结束）
- 会话 ID 和安全定位符：session_id `01a0a6d7-3aed-7a53-97a3-a1617a53be0f`；`%USERPROFILE%\.codex\sessions\2026\09\16\rollout-2026-09-16T04-51-58-01a0a6d7-3aed-7a53-97a3-a1617a53be0f.jsonl`
- 技术领域和任务类型：非 GPU 通用软件工程 / 本地数据清点与结构化产出（只读扫描 + 报告与打包）
- 最终状态：未验证（扫描时仍在进行）

## 任务定义
- 既有工程背景：用户以粘贴文本方式给出完整规则，要求对 `%USERPROFILE%\.codex` 下授权的会话子目录做只读清点，并输出索引、报告、重复组与推荐轨迹 ZIP。
- 初始问题：缺少对本地 Coding Agent 会话的系统盘点与可用于提交的轨迹整理。
- 最终目标和约束：清点全部可识别任务并梳理支撑材料；检查硬门槛、复杂度、Workspace 完备性与重复性；仅为“推荐提交”候选生成规定结构的独立 ZIP；全程只读，不创作或改写原始对话。

## 人机协作
- 用户新增信息（如有；不要求多轮补充）：单轮下发完整 Prompt 与参数（范围、输出目录、最多推荐数、是否复制 Workspace 文件、是否生成 ZIP）。
- Agent 关键观察、行动和调整：进入授权会话目录做递归清点，解析原生 rollout JSONL 并调用本地工具完成统计；扫描期间未出现用户补充或纠正。

## 验收条件
| 核心要求 | 可观察结果 | 验证证据 | 当前状态 |
| --- | --- | --- | --- |
| 清点并导出全部可识别任务 | 生成 `task_index.csv` / `task_index.jsonl` | 输出目录内索引文件 | 进行中 |
| 输出总报告与重复组 | 生成 `screening_report.md` / `duplicate_groups.json` | 输出目录内报告文件 | 进行中 |
| 仅为推荐候选生成 ZIP | `packages/<trajectory_id>.zip` | 输出目录 packages/ | 进行中 |
| 只读原始材料 | 未修改原始会话与 Workspace | 无写操作证据 | 进行中 |

## 支撑材料
| 材料 | 用途 | 完备状态 | 安全定位符 |
| --- | --- | --- | --- |
| 原生会话 JSONL | 原始轨迹 | 完整（持续增长） | `%USERPROFILE%\.codex\sessions\2026\09\16\rollout-2026-09-16T04-51-58-01a0a6d7-3aed-7a53-97a3-a1617a53be0f.jsonl` |
| 用户粘贴文本 | 任务输入 | 完整 | 授权附件目录下粘贴文件 |
| 附件 | — | 无（无独立图片/文件附件） | — |

## 评价
- Workspace 完备性：证据不足（会话未结束，产出集合未定）
- 复杂度：证据不足
- 七个价值方向：工程价值 中 / 专业挑战 中 / Agentic 深度 中 / 专家贡献 弱 / 任务闭环 弱 / 环境质量 中 / 验证价值 弱
- 候选层级：条件候选

## 结论
- 分类：补充或脱敏后再提交
- 缺口和风险：会话在扫描时仍在进行，缺少最终状态与验收结果；原生文件持续增长。
- 提交前动作：待会话结束后重新采集原始文件，复核终态与产出完整性后再判定。
